from django.urls import path

urlpatterns = [
    # * path('profile/<int:profile_id>/', ),
    # * path('topics/', ),
    # * path('topics/<int:topic_id>/', ),
    # * path('topics/<int:topic_id>/posts/', ),
    # * path('new-posts/', ),
    # * path('posts/<int:post_id>/', ),
    # * path('posts/<int:post_id>/comments/', ),
    # * path('forum-statistics/', ),
    # * path('login/', ),
    # * path('register/', ),
]
